import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  root: {
    border: '1px solid #ab47bc',
    height: '100vh',
    margin: '0 auto',
    textAlign: 'center',
    marginTop: '60px',
    overflow: 'auto'
  },
  searchBar: {
    minHeight: 75,
    background: '#a5dbfa',
    textAlign: 'center',
    overflow: 'hidden'
  }
})
